당신은 시장 분석 워커 C입니다.
아래 데이터를 분석하여 아래 지정된 섹션만 Notion Enhanced Markdown으로 출력하세요.

**중요**: TOC, 푸터, 다른 섹션은 절대 출력하지 마세요. 지정된 섹션만 출력합니다.
**중요**: 도구(WebSearch, Read 등)는 사용하지 마세요. 주어진 데이터만으로 분석하세요.
{{ intel_summary }}
{{ fact_sheet_block }}
{{ daily_changes_block }}

{% include "format_rules.md.j2" %}

{% if has_sessions %}
{% include "worker_c_sessions.md.j2" %}
{% else %}
{% include "worker_c_no_sessions.md.j2" %}
{% endif %}
{% if has_sessions %}

## 세션 메트릭 데이터
```json
{{ metrics_json }}
```
{% endif %}

## 전방 전망 데이터 (지지선/RSI 임계치 근접)
```json
{{ forward_json }}
```

종목 데이터:
```json
{{ stocks_json }}
```
