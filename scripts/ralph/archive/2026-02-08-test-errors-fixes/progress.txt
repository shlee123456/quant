# Ralph Progress Log
Started: Sun Feb  8 10:00:04 KST 2026

## Codebase Patterns
- Strategy position tracking: Use 0 (flat) or 1 (long) only, never -1. Signal can be -1/0/1 for SELL/HOLD/BUY
- Empty DataFrame handling: Add `if df.empty: return df.copy()` at start of calculate_indicators methods
- Empty list handling for DataFrames: Add `if not list: return pd.DataFrame()` before processing to avoid sort_values KeyError
- Optional parameter with computed default: Move param to end with None default, then `self.param = param if param is not None else computed_value`
- Mock lazy imports: Patch at import source (e.g., `@patch('pykis.PyKis')`) not module level for imports inside functions
- Random seed reproducibility: Use `np.random.RandomState(seed)` as instance variable, not global `np.random.seed(seed)`
- Reproducible timestamps: Use fixed date (e.g., datetime(2024,1,1)) when seed provided, not datetime.now()
- RSI signal generation requires crossovers, not just threshold violations (e.g., RSI < 30 AND prev RSI >= 30)
- Test data arrays must match date range length exactly for DataFrame creation
- Pre-existing typecheck errors (pandas stubs, config.py, paper_trader.py, pykis) should not block story completion
- Terminal logs saved to `.context/terminal/` with timestamps for all bash commands
- Pytest fixture for external APIs: Load from env vars, pytest.skip() if missing, handle ImportError for optional dependencies
- Rate limit handling pattern: Wrap API calls in try/except, check error message for keywords, pytest.skip() if rate limited
- Pytest slow marker: Create pytest.ini, register marker, apply @pytest.mark.slow to API test classes, use `pytest -m "not slow"` to skip
- Dashboard UI testing pattern: Test data structures/workflows, not rendering. Mock Streamlit components and external dependencies (brokers, data handlers)
- Strategy-agnostic code pattern: Check info dict for available fields dynamically instead of hardcoding strategy-specific indicators (e.g., check `if 'fast_ma' in info` before accessing)
---
## 2026-02-08 10:15 - US-101
- Implemented position tracking fix in RSI, MACD, Bollinger Bands strategies
- Files changed:
  - trading_bot/strategies/rsi_strategy.py
  - trading_bot/strategies/macd_strategy.py
  - trading_bot/strategies/bollinger_bands_strategy.py
- **Learnings for future iterations:**
  - Position tracking logic pattern: BUY (1) → position=1, SELL (-1) → position=0, HOLD (0) → maintain previous
  - The old logic using `data['signal'].replace(0, np.nan).ffill().fillna(0)` caused -1 values in position column
  - Position must only contain 0 or 1 for backtesting to work correctly
  - Test failures in other tests (empty dataframe, test data generation) are separate issues (US-102, US-103)
  - Mypy typecheck shows pre-existing errors in other files, not related to this fix
---
## 2026-02-08 10:30 - US-102
- Implemented empty DataFrame handling in RSI, MACD, MA strategies
- Files changed:
  - trading_bot/strategies/rsi_strategy.py
  - trading_bot/strategies/macd_strategy.py
  - trading_bot/strategy.py
- **Learnings for future iterations:**
  - Empty DataFrame handling pattern: Add early return check `if df.empty: return df.copy()` at the start of calculate_indicators
  - Bollinger Bands and Stochastic strategies already had this check, so only RSI, MACD, and MA needed updates
  - Tests expect empty input to return empty output (not a DataFrame with default columns)
  - Typecheck errors in other files (config.py, paper_trader.py, etc.) are pre-existing and not related to this fix
  - All 4 empty_dataframe tests now pass (RSI, MACD, Bollinger Bands, Stochastic)
---
## 2026-02-08 10:45 - US-103
- Fixed MACD and RSI test data generation errors
- Files changed:
  - tests/test_macd_strategy.py
  - tests/test_rsi_strategy.py
- **Learnings for future iterations:**
  - MACD test data issue: Price arrays must match the date range length (e.g., 100-period dates need 100 prices)
  - RSI test data issue: Linear price movements (all gains/all losses) produce extreme RSI (0 or 100) but NO signals
  - RSI strategy requires CROSSOVERS to generate signals:
    * BUY: RSI < 30 AND previous RSI >= 30 (crosses DOWN through oversold threshold)
    * SELL: RSI > 70 AND previous RSI <= 70 (crosses UP through overbought threshold)
  - To generate RSI test signals, use pattern: sideways → sharp move → recovery (creates crossover)
  - Example: For oversold signal: neutral start → sharp decline (RSI drops below 30) → recovery (RSI crosses back above 30, triggers BUY)
  - Typecheck errors in other files (config.py, paper_trader.py, etc.) are pre-existing project-wide issues, not related to test fixes
---
## 2026-02-08 11:00 - US-104
- Fixed optimizer empty strategy list handling
- Files changed:
  - trading_bot/optimizer.py
- **Learnings for future iterations:**
  - Empty list handling pattern: Add early return check `if not strategies: return pd.DataFrame()` before processing
  - The `compare_strategies` method was failing with KeyError when trying to sort an empty DataFrame by 'total_return' column
  - Empty DataFrame has no columns, so `sort_values()` will fail if called on it
  - Pattern applies to any function that processes a list and returns a DataFrame - check for empty input first
  - All optimizer tests (20 tests) pass after this fix
  - Pre-existing typecheck errors in other files remain unchanged
---
## 2026-02-08 11:30 - US-106
- Fixed KoreaInvestmentBroker initialization parameter handling
- Files changed:
  - trading_bot/brokers/korea_investment_broker.py
  - tests/test_brokers.py
- **Learnings for future iterations:**
  - Optional parameter pattern: Move optional param to end with default value (user_id after account with default=None)
  - When making parameter optional that defaults to another parameter's value, use pattern: `self.param = param if param is not None else other_param`
  - PyKis is imported inside __init__ (lazy loading), so it's not at module level - must patch at import location: `@patch('pykis.PyKis')`
  - When broker was a skeleton (old tests expected NotImplementedError), but now it's implemented - update tests to match reality
  - Mock setup pattern: Create mock instance, set return_value, patch at import source (not module level)
  - Tests using real credentials via fixtures (test_korea_investment_broker.py) don't need changes if they already pass user_id
  - All broker tests in test_brokers.py now pass (4 tests), test_korea_investment_broker.py also works
---
## 2026-02-08 12:00 - US-105
- Fixed simulation data seed reproducibility
- Files changed:
  - trading_bot/simulation_data.py
- **Learnings for future iterations:**
  - Random seed reproducibility requires instance-level random state, not global state
  - Use `np.random.RandomState(seed)` instead of `np.random.seed(seed)` for reproducible random generation
  - Store RandomState as `self.rng` and use `self.rng.normal()` instead of `np.random.normal()`
  - Fixed timestamp reproducibility: Use fixed date (datetime(2024,1,1)) when seed is provided instead of datetime.now()
  - Pattern: `if self.seed is not None: start_date = datetime(2024,1,1) - offset` for reproducible timestamps
  - All 23 simulation data tests pass after fix
  - Replace ALL np.random calls with self.rng calls throughout the class for full reproducibility
---
## 2026-02-08 16:00 - US-107
- Implemented KIS API pytest fixture for proper test integration
- Files changed:
  - tests/conftest.py (created)
  - tests/test_kis_api.py (converted to pytest format)
- **Learnings for future iterations:**
  - Pytest fixture pattern for external API clients: Load credentials from env vars, skip if missing
  - conftest.py is automatically discovered by pytest and makes fixtures available to all tests
  - For optional dependencies (like pykis), wrap import in try/except and pytest.skip() on ImportError
  - Test file can have both pytest tests (for CI/CD) and main() function (for manual testing)
  - KIS API has rate limit: 1 token request per minute - this will be addressed in US-108
  - Pre-existing typecheck error for pykis (no type stubs) is expected and not blocking
  - Pattern for credential fixture:
    ```python
    @pytest.fixture
    def api_client():
        credentials = load_from_env()
        if not all(credentials.values()):
            pytest.skip("credentials not set")
        return APIClient(**credentials)
    ```
---
## 2026-02-08 17:30 - US-108
- Implemented API rate limit handling for KIS API tests
- Files changed:
  - tests/test_korea_investment_broker.py (added @pytest.mark.slow, sleep, rate limit error handling)
  - pytest.ini (created, registered slow marker)
  - README.md (added test markers documentation)
- **Learnings for future iterations:**
  - KIS API has strict rate limit: 1 token request per minute (EGW00133 error code)
  - Rate limit error pattern: Check for multiple keywords ('rate limit', 'egw00133', '1분당 1회', 'forbidden')
  - Pytest marker pattern: Create pytest.ini with marker definition, apply @pytest.mark.slow to test classes
  - Rate limit skip pattern: Wrap API calls in try/except, check error message, pytest.skip() if rate limited
  - Sleep between consecutive API calls in loops: `if i > 0: time.sleep(1)` pattern
  - Test marker usage: `pytest -m slow` (only slow), `pytest -m "not slow"` (exclude slow)
  - Pre-existing test failures (test_kis_api.py, test_strategy.py) are unrelated to this story
  - All 7 tests pass, 7 skipped due to rate limits - this is expected behavior
---
## 2026-02-08 10:37 - US-109
- Implemented automated integration test script
- Files changed:
  - scripts/run_integration_tests.sh (created)
  - tests/CLAUDE.md (documentation update from US-107, US-108)
- **Learnings for future iterations:**
  - Integration test script pattern: Use bash with `set -e` for exit on error, color codes for readable output
  - Test logging pattern: Create timestamped logs in .context/terminal/ with descriptive names (integration_backtest_<timestamp>.log)
  - Summary log pattern: Append [PASS]/[FAIL] results to separate summary file for easy review
  - Test runner pattern: Use `|| true` after run_test calls to continue on failure, track counts, exit with error if any failed
  - tee command usage: `command 2>&1 | tee logfile` captures both stdout/stderr while displaying to console
  - Shell script testing: Scripts run all 5 integration tests (backtest, optimization, comparison, dashboard, quickstart)
  - All tests passed successfully: 5/5 tests completed with proper logging
  - Pre-existing numpy warnings (mean of empty slice) in optimization are expected and don't affect test success
  - Generated output files (docs/*.json, docs/*.csv) should not be committed - they're artifacts from running examples
---
## 2026-02-08 18:30 - US-110
- Implemented comprehensive dashboard UI tests
- Files changed:
  - tests/test_dashboard_ui.py (created)
- **Learnings for future iterations:**
  - Dashboard test structure: Organize tests by functional area (Tabs, Charts, ErrorHandler, DataFlow)
  - Tab testing pattern: Test data structures/workflows rather than rendering (can't test Streamlit rendering without browser)
  - Chart testing pattern: Verify fig object is not None and has traces (len(fig.data) > 0)
  - Error handler testing pattern: Mock streamlit (st) module and verify error display methods are called
  - Test all 5 strategy chart types: MA, RSI, MACD, Bollinger Bands, Stochastic
  - ChartGenerator methods to test: plot_equity_curve, plot_price_with_signals, plot_price_with_ma, plot_drawdown, plot_trade_analysis, plot_strategy_chart (with strategy-specific variants)
  - Error type identification uses keyword matching in error message (lowercase comparison)
  - Use MagicMock for context managers: mock_st.expander = MagicMock(return_value=MagicMock(__enter__=..., __exit__=...))
  - All 26 tests pass successfully
  - Pre-existing typecheck errors (pandas stubs, plotly stubs) remain unchanged
---
## 2026-02-08 19:00 - US-111
- Implemented centralized logging system
- Files changed:
  - trading_bot/logging_config.py (created)
  - trading_bot/backtester.py (added logging)
  - trading_bot/brokers/korea_investment_broker.py (added logging)
  - trading_bot/strategies/rsi_strategy.py (added logging)
- **Learnings for future iterations:**
  - Logging pattern: Create logging_config.py with setup_logger() and pre-configured module loggers
  - Logger setup: Dual handlers (console + file), different formats for each (console: simple, file: detailed with timestamps)
  - File logging pattern: Timestamped log files in .context/logs/ with module name (e.g., trading_bot_backtester_20260208.log)
  - Exception logging: Use log_exception(logger, message) helper to automatically include stack trace
  - Module logger pattern: Import logger at module level: `from ..logging_config import get_xxx_logger` then `logger = get_xxx_logger()`
  - Backtester logging: Wrap entire run() method in try/except, log start/completion/errors
  - Broker logging: Log initialization, authentication errors, rate limit warnings
  - Strategy logging: Log initialization with parameters (use DEBUG level)
  - Pre-configured loggers: get_broker_logger(), get_strategy_logger(), get_backtester_logger(), etc.
  - Avoid duplicate handlers: Check `if logger.handlers:` before adding handlers
  - All tests pass (191 passed, 2 pre-existing failures)
  - Pre-existing typecheck errors (pandas stubs) remain unchanged
---
## 2026-02-08 20:00 - US-112
- Implemented comprehensive tests for Live Monitor functionality
- Files changed:
  - tests/test_live_monitoring.py (created)
  - scripts/ralph/prd.json (updated)
- **Learnings for future iterations:**
  - Live Monitor testing pattern: Test data structures and workflows rather than rendering (Streamlit UI can't be directly tested without browser)
  - Current Market Price section is only shown when: `market_type == 'stock' AND use_simulation == False`
  - Auto-refresh pattern: Track state with `auto_refresh_enabled`, `last_refresh_time`, calculate elapsed/remaining time, trigger on `elapsed >= interval`
  - KIS broker ticker fetch pattern: `broker.fetch_ticker(symbol, overseas=True, market='NASDAQ')` returns dict with 'last', 'rate', 'open', 'high', 'low', 'volume'
  - Symbol normalization: Remove market suffix (e.g., 'AAPL.US' -> 'AAPL') before calling fetch_ticker
  - Price formatting: 2 decimal places for prices (f"${value:.2f}"), comma separators for volume (f"{int(value):,}")
  - Delta color logic: "normal" for rate >= 0, "inverse" for rate < 0
  - Test organization: Group tests by functional area (CurrentPriceSection, AutoRefresh, KISBrokerQuotes, DataFlow)
  - Mock external dependencies: Use @patch for KoreaInvestmentBroker and DataHandler to avoid API calls
  - All 15 tests pass successfully
  - Pre-existing typecheck errors (pandas stubs) are expected and not blocking
---
## 2026-02-08 21:00 - US-113
- Implemented comprehensive end-to-end tests for paper trading
- Files changed:
  - tests/test_paper_trading_e2e.py (created - 15 comprehensive tests)
  - trading_bot/paper_trader.py (fixed _print_status to be strategy-agnostic)
- **Learnings for future iterations:**
  - Paper trading E2E test pattern: Test initialization → buy execution → sell execution → portfolio tracking → strategy integration
  - PaperTrader was tightly coupled to MovingAverageCrossover (hardcoded 'fast_ma', 'slow_ma' in _print_status)
  - Strategy-agnostic print pattern: Check info dict for available fields dynamically instead of assuming specific strategy indicators
  - E2E test structure: Group tests by functional area (Initialization, Orders, Cycles, Integration, EdgeCases)
  - Test fixtures pattern: Create reusable simulation_data, trending_data, mock_data_handler fixtures for consistent test data
  - Portfolio value calculation: capital + (position * current_price) when position > 0, else just capital
  - Commission impact testing: Use high commission (1%) to make effects clearly visible in tests
  - Duplicate signal handling: PaperTrader tracks last_signal to avoid duplicate trades on same signal
  - All 15 tests pass successfully, covering full paper trading workflow
  - Pre-existing test failures (test_kis_api.py, test_strategy.py) remain unchanged
---
